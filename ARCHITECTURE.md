# ArgusOS Architecture

See `design/ARCHITECTURE.md` for the current, accurate architecture. This file previously contained a description of a pre-Factory prototype that has since been retired (`design/decisions/0004_REMOVE_LEGACY_PROTOTYPE.md`) and was left stale relative to the real, 41-package Core system. Kept as a pointer rather than left empty, per the discoverability pattern in `factory/standards/FRAMEWORK_DOCUMENT_LOCATION_CONVENTION.md` — this file does not restate architecture, it only locates it.
